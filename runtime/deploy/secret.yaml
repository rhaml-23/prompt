apiVersion: v1
kind: Secret
metadata:
  name: compliance-fleet-secrets
  namespace: compliance-fleet
  labels:
    app.kubernetes.io/part-of: compliance-fleet
type: Opaque
stringData:
  # Replace with actual values before applying
  DATABASE_URL: "postgresql://compliance:CHANGEME@postgresql:5432/compliance_fleet"
  NATS_URL: "nats://nats:4222"
  S3_BUCKET: "compliance-fleet-state"
  S3_ENDPOINT: "https://s3.example.com"
  S3_ACCESS_KEY: "CHANGEME"
  S3_SECRET_KEY: "CHANGEME"
  LLM_API_KEY: "CHANGEME"
  LLM_API_ENDPOINT: "https://api.example.com/v1"
